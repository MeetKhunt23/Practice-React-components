// prop drilling as option instead of redux but we have to pass props to every child component
// usecontext hook is also used as state management tool instead of using redux
// we are using redux toolkit for working with async operations in redux because old redux does not support with async operations so toolkit are usually help in many way.

/*
key points:
store 
action
reducer

*/


